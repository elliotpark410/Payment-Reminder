import React from 'react';

function LessonHistory() {
  const showHistory = () => {
    // Logic to show lesson history
  };

  return (
    <div className="lesson-history">
      <button onClick={showHistory}>History</button>
    </div>
  );
}

export default LessonHistory;
